import {
  saveNewReference,
  saveUserDetails,
  saveSingleUserDetails,
  saveCurrentAddress,
} from './action';
import reducers from './reducer';
export {
  saveNewReference,
  saveUserDetails,
  saveSingleUserDetails,
  saveCurrentAddress,
};

export default reducers;
