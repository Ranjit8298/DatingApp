import {
  saveNewReference,
  saveUserDetails,
  saveSingleUserDetails,
  saveCurrentAddress,
  saveMode,
  saveMobileNumber,
  Logout,
} from './action';
import reducers from './reducer';
export {
  saveNewReference,
  saveUserDetails,
  saveSingleUserDetails,
  saveCurrentAddress,
  saveMode,
  saveMobileNumber,
  Logout,
};

export default reducers;
